rearrange a string so that all same characters become at least d distance away.

given a string and distance d:

str="aabbcc" d=3
out="abcabc"

solution:

1. hash table counting occurence.
2. similar to top K, max_heap while counting.
3. from most occurence to least
