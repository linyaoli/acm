#!/bin/bash
# auto committing all leet files.

git add .
echo "*****  All files loaded *****"
git status
echo "*****  change log *****"
now="$(date + "%T")"
echo $now
git commit -m "$now"
echo "* uploading ...  *"
git push
echo "DONE !"
